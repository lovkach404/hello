написать следующие методы для класса string:

1. startswith - возвращает true если строка начинается с переданной строки иначе false
2. endswith - возвращает true если строка заканчивается переданной строкой иначе false
3. isalpha - возвращает true если строка состоит только из букв иначе false